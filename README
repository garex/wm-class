
Purpose:

  Setting the WM_CLASS property by PID or by running a program.

Compile:

  $ gcc wm_class.c -o wm_class -lX11

Usage:

  $ wm_class --set <name> <class> <pid>
  $ wm_class --run <name> <class> <program> <argument>*
